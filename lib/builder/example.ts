import {newDocument,newSection,newStep} from './model';
export function exampleDocument(){const d=newDocument();d.id='example-member-pickup';d.title='Foreign currency member pickup';d.audience='Branch team members';d.owner='Branch Operations';d.effectiveDate='2026-09-10';d.purpose='Provide a consistent, secure process for handing over foreign currency orders to members.';d.fields.when_to_use=['A member arrives at the branch to collect their foreign currency order.'];const a=newSection();a.title='Verify the member and order';a.purpose='Confirm the order is ready and the member is authorized to collect it.';a.steps=['Ask the member for a valid government-issued photo ID.','Locate the order in the currency ordering system.','Match the member’s details to the order confirmation.'].map(instruction=>({...newStep(),instruction}));a.notes=['Keep the order reference available throughout the pickup process.'];a.decisions=[{id:'example-decision',condition:'The member’s details do not match the order',result:'Pause the pickup and contact the branch manager.',original:''}];const b=newSection();b.title='Complete the pickup';b.steps=['Count the currency with the member.','Record the collection and provide a receipt.'].map(instruction=>({...newStep(),instruction}));b.warnings=['Do not release currency until identity verification is complete.'];b.expectedResult='The member receives their order and the collection is recorded.';d.procedures=[a,b];return d;}
