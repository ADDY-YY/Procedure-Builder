# Security

Authored and imported text is treated as untrusted. Rendering and export metadata escape markup, and links only permit safe HTTP(S), mailto, and image data URLs where needed. Raw HTML is not an authoring format and no renderer or export evaluates input as code.

The MVP has no external analytics, public CDN, or public runtime dependency. Local drafts stay in the browser profile unless the author downloads or shares an export.
