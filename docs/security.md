# Security

Authored and imported text is treated as untrusted. Rendering escapes markup and only permits safe HTTP(S), mailto, and image data URLs where needed. Raw HTML is not an authoring format and no renderer evaluates input as code.

The MVP has no external analytics, public CDN, or public runtime dependency. Local drafts stay in the browser profile unless the author downloads or shares an export.
