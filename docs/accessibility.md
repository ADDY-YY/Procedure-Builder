# Accessibility

Procedure Builder uses semantic native controls, labels, headings, details/summary accordions, button names, focus-visible styling, live save messages, and structured tables in rendered procedures. The editor should remain usable with keyboard navigation, browser zoom, and screen readers.

Before release, test keyboard-only operation, dialog focus/escape behavior, 200% text scaling, color contrast, screen-reader labels, and validation navigation in supported browsers.
